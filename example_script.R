
##### EXAMPLE R SCRIPT ####
# This is an example R script illustrating how to push new files. It doesn't do anything,
# but here is a random reminder why you should always use TRUE instead of T:

TRUE == TRUE
TRUE == T

T <- 42
TRUE == T
